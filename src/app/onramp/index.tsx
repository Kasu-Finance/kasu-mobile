/**
 * Standalone route for the Wayex fiat on/off-ramp. The same `OnrampScreen` is
 * also mounted into the Card tab during integration; this route lets the flow
 * be linked to directly (e.g. `/onramp`).
 */
export { default } from '@/features/onramp/onramp-screen';
