/**
 * Standalone route for the lending feature. The same `LendingScreen` is also
 * mounted into the Lend tab during integration; this route lets the flow be
 * linked to directly (e.g. `/lending`).
 */
export { default } from '@/features/lending/lending-screen';
